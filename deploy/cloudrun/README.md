# Deprecated Cloud Run package

`deploy/digitalocean/` is the active local deployment-preparation package for
the restricted pilot. These Cloud Run scripts are retained as historical
evidence only and must not be used for a new pilot deployment.
