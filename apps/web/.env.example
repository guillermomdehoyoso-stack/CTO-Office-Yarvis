# Local web transport example. Put actual local values in ignored .env.local.
VITE_API_BASE_URL=http://localhost:8000
# Productive authority comes only from the backend session cookie. Do not add
# subjects, organization selectors, capabilities, or bearer tokens to Vite.
