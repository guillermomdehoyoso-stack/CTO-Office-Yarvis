import {Link} from 'react-router-dom'; import type {StoreIntelligenceSummary} from '../../types/storeIntelligence';
export function MissionControlStoreIntelligenceCard({data,error}:{data?:StoreIntelligenceSummary;error?:boolean}){if(error)return <p>Store Intelligence no está disponible.</p>;if(!data)return <p>Cargando Store Intelligence…</p>;return <section><h2>Store Intelligence</h2><Link to="/recovery-queue">Atención requerida: {data.pending_profiles}</Link><p>Críticos: {data.activation_failures+data.operational_blocks} · Revisión: {data.evidence_review_profiles} · Contexto histórico: {data.historical_only_profiles}</p></section>}
