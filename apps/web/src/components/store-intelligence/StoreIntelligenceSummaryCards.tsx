import type {StoreIntelligenceSummary} from '../../types/storeIntelligence';
export function StoreIntelligenceSummaryCards({data}:{data?:StoreIntelligenceSummary}){if(!data)return <p>Cargando resumen…</p>;return <section className="cards">{[['Atención requerida',data.pending_profiles],['Revisión de evidencia',data.evidence_review_profiles],['Activación pendiente',data.activation_failures],['Bloqueo operativo',data.operational_blocks],['Contexto histórico',data.historical_only_profiles]].map(([k,v])=><div className="card" key={String(k)}><small>{k}</small><b>{String(v)}</b></div>)}</section>}
